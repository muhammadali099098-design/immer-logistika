# Изменения от 17.08.2026

## 1. Кнопки «Скачать Excel» — теперь настоящие .xlsx
Раньше все «Excel»-кнопки скачивали CSV, который Excel открывал как
сплошной текст в одной ячейке (и информация выглядела неверной).
Теперь формируются настоящие файлы Excel (.xlsx) через библиотеку
SheetJS (лежит в `public/vendor/`): правильные колонки, числа — как числа,
даты — как даты, ширина колонок настроена.

- **Финансы** → файл с 3 листами: «Финансы» (все фуры с долгом и сроками),
  «Сводка по логистам» (оплачено/долг/%), «Товары» (каждая позиция отдельной
  строкой с кубом).
- **Сводка: Завод-Граница** → листы «Завод-Граница» (с итогами) и «Фуры»
  (все загрузки и отгрузки по строкам).
- **«Товар фуры»** в модалке → «Скачать Excel» вместо CSV.

## 2. Завод-Граница — кнопка «все (N)» в колонке «Отгружено»
Кнопка показывала список НЕ той фуры или ошибку. Исправлено: теперь клик
открывает модалку с полным списком фур именно этой строки.

## 3. Финансы — компактная панель
Кнопки «Оплачено / Общий долг / Скачать Excel / Оплата разом» ужаты в одну
невысокую панель над таблицей и больше не занимают место. Массовая оплата
появляется в ней, только когда отмечены фуры галочками.

## 4. Общие отчёты — новая инфографика
- «Ближайший платёж» — фура и дата с ближайшим сроком оплаты.
- «Просрочено долга» — сумма к оплате сейчас.
- «К оплате за 7 дней» — фур и сумма с наступающим сроком.
- «Оплачено фур» — доля целиком оплаченных.
- Карточка «Дни до оплаты»: распределение долга по срокам
  (просрочено / 1–7 / 8–14 / 15–30 / 30+ дней) с полосой и суммами.
- Карточка «Просрочки по логистам»: у кого просроченный долг и сколько.

## 5. Прочее (проверка всего сайта)
- «Дней до оплаты» и «Срок оплаты» считались с ошибкой в 1 день из-за
  перевода времени в UTC — исправлено (календарный расчёт, без сдвига).
- Исправлен скрытый сбой `/api/stats` (обращение к несуществующей таблице
  «loadings») — раньше статистика на входе падала молча.
- Синтаксис и все маршруты проверены автотестами (серверные API +
  клиентские UI-проверки + проверка реальных .xlsx-файлов).

## Развёртывание
1. Замените файлы в репозитории GitHub этим архивом (новый файл —
   `public/vendor/xlsx.full.min.js`).
2. Render пересоберёт автоматически (npm install → npm start).
   Дополнительных переменных окружения не нужно.
# Обновление от 17.08.2026 (вторая серия правок)

## 6. «Зависание» при удалении и «выход из аккаунта» после обновления
Причина: каждая запись в Google-таблицу сбрасывала ВЕСЬ кэш, поэтому после
удалений (особенно нескольких позиций подряд) лимиты Google давали сбои —
сайт «подвисал», проверка сессии падала и показывала форму входа, а сам вход
выдавал ошибку. Исправлено:
- кэш теперь сбрасывается точечно (только по затронутой таблице) — запросов
  к Google стало в разы меньше;
- если Google ответил лимитом/сбоем при чтении — отдаются последние
  известные данные, сессия не теряется;
- запись при лимите спокойно повторяется с паузами;
- при старте сайта проверка сессии повторяется автоматически, а при
  временном сбое сервера показывается окно «Сервер просыпается — аккаунт не
  потерян» с авто-повтором, вместо формы входа;
- у всех удалений появляется подсказка «Удаляю…», чтобы не казалось,
  что сайт завис.

## 7. Первый раздел — «Общие отчёты»
После входа (и при обновлении страницы, если вы уже вошли) теперь сразу
открываются «Общие отчёты», а не «Сводка: Завод-Граница».

# Обновление от 17.08.2026 (третья серия правок)

## 8. Завод-Граница — вкладка «Отправленные позиции»
- В основной таблице теперь видны только позиции с остатком на складе.
- Кнопка «Отправленные позиции (N)» открывает список позиций, которые уже
  полностью отгружены или списаны. Рядом — «Скачать Excel» (выгружает
  именно текущий список, включая отправленные).
- Автоматическое списание: если по модели у логиста осталось ≤ 5 шт,
  после отгрузки этот остаток списывается в 0 и позиция уходит
  в «Отправленные». Пример: было 60 шт, отгрузили 56 → остаток 4 → в
  производстве «0», в «Разнице» видно −4.

## 9. Граница → Ташкент
- Убраны блоки «Логисты — нажмите…» и «Логистов: · Фур:» — страница сразу
  показывает таблицу отгрузок.
- Рядом с «Записать отгрузку» добавлена кнопка «Скачать Excel»
  (лист «Отгрузки» + лист «По моделям» с каждой позицией отдельно).
- Добавлен столбец «Разница» (после «Товар»):
  * «+N» — привезли больше, чем было у логиста (например, 601 вместо 600 → +1);
  * «−N» (до −5) — дозабрали последние штуки (56 из 60 → −4), остаток списывается;
  * «—» — обычная отгрузка.
- Ошибка «Нельзя отгрузить больше остатка» убрана: превышение количества
  теперь разрешено и фиксируется в «Разнице».

## 10. Финансы
- Кнопки «Скачать Excel» и «Оплата разом» перенесены вправо в верхний
  угол, как в остальных разделах. При отметке фур галочками слева
  появляется «Оплатить выбранное (N)» с суммой долга.
